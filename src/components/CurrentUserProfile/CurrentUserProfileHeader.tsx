import { Paper, styled } from "@mui/material";

const CurrentUserProfileHeader = styled(Paper)({
  borderRadius: 0,
  boxShadow: "-4px 10px 40px rgba(44, 63, 88, 0.05)",
});

export default CurrentUserProfileHeader;
